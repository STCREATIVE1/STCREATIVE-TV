# 📺 TVFZF - Terminal TV Player

A lightweight Bash script to stream live TV channels using `fzf` and `mpv`.

## 🛠️ Requirements
Make sure you have these installed:
* `fzf` (For the menu interface)
* `curl` (To fetch channel lists)
* `mpv` or `vlc` (To play the video)

## 🚀 Installation & Usage
1. Clone the repo:
   ```bash
   git clone [https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git](https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git)
   cd YOUR_REPO_NAME
   
